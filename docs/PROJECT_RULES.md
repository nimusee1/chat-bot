# 프로젝트 규칙 & 구조 가이드

본 문서는 프로젝트의 `/CLAUDE.md` 규칙을 준수하기 위한 가이드입니다.

## 📋 파일 구조 규칙

### CLAUDE.md 준수 사항

✅ **준수 완료:**

#### 1. 문서 관리
```
docs/
├── README.md           # 설치 및 사용 가이드
├── SETUP.md           # 개발 환경 설정 & 배포
├── ARCHITECTURE.md    # 시스템 아키텍처
├── DEVELOPMENT.md     # 개발 가이드
└── PROJECT_RULES.md   # 이 파일
```

**규칙**: `/docs/` 에 모든 문서 (.md) 관리
- ✅ README.md가 docs/에 위치
- ✅ 추가 문서들도 docs/에 중앙 관리
- ✅ 프로젝트 루트는 최소한으로 유지 (PROJECT_OVERVIEW.md만 예외)

#### 2. 프로젝트 구조

```
chat-bot/
├── docs/                    # 📚 모든 마크다운 문서 관리
├── src/                     # 소스 코드
│   ├── components/         # React 컴포넌트
│   ├── lib/               # 유틸리티 함수
│   ├── server/            # Express 백엔드
│   ├── prompts/           # 시스템 프롬프트
│   └── styles/            # 글로벌 스타일
├── public/                # 정적 자산
├── key.json              # API 키 (민감정보)
├── package.json          # npm 설정
├── tsconfig.json         # TypeScript 설정
├── vite.config.ts        # Vite 설정
├── PROJECT_OVERVIEW.md   # 프로젝트 개요 (루트 예외)
└── .gitignore           # Git 제외 파일
```

**규칙 준수:**
- ✅ 문서는 `/docs/`에만 위치
- ✅ 스크립트는 없음 (향후 필요시 `/scripts/`에 추가)
- ✅ Docker 없음 (향후 필요시 `/docker/`에 추가)
- ✅ 루트는 최소한으로 유지 (설정파일만)

## 🎯 요구사항 명확화 프로토콜

### 적용 사항

본 프로젝트 개발 중 요구사항 명확화:

| 항목 | 결정 |
|------|------|
| **프론트엔드 선택** | Pure React + Vite (Next.js 대신) |
| **백엔드 프레임워크** | Express.js (통합 구조) |
| **상태 관리** | React Hooks + localStorage |
| **스타일링** | CSS Modules (Tailwind 대신) |
| **데이터베이스** | 없음 (localStorage만 사용) |
| **인증** | 없음 (로컬 접근만) |
| **배포** | Standalone (Docker 옵션) |

### 향후 결정이 필요한 사항

다음 기능 추가 시 AskUserQuestion 사용:
- [ ] 사용자 인증 방식 (JWT, OAuth 등)
- [ ] 데이터베이스 (PostgreSQL, MongoDB 등)
- [ ] 배포 환경 (Vercel, AWS, GCP 등)
- [ ] CI/CD 파이프라인 (GitHub Actions, GitLab CI 등)
- [ ] 모니터링 도구 (Sentry, DataDog 등)

## 📝 새 기능 추가 시 체크리스트

### 1. 계획 단계

- [ ] 요구사항 명확화 (필요시 AskUserQuestion 사용)
- [ ] 디자인 검토 (아키텍처 영향도 확인)
- [ ] 기존 코드와의 호환성 검토

### 2. 구현 단계

- [ ] TypeScript 타입 명시
- [ ] 테스트 추가 (수동 테스트 체크리스트)
- [ ] 문서 업데이트
- [ ] 코드 스타일 가이드 준수

### 3. 마무리 단계

- [ ] lint 통과
- [ ] 코드 리뷰
- [ ] docs/ 문서 최신화
- [ ] Git 커밋 메시지 작성

## 🔒 보안 민감 파일

### Git 제외 파일 (.gitignore)

```
# API Keys & Environment
key.json                 # ⚠️ Gemini API 키
.env
.env.local
.env.*.local

# 기타 민감정보
secrets/
credentials/
```

**주의**: 이 파일들은 절대 커밋하지 마세요!

## 📚 문서 유지보수

### 문서 버전 관리

각 문서의 업데이트 범위:

| 문서 | 담당 범위 | 빈도 |
|------|----------|------|
| docs/README.md | 설치/사용법 | 변경 시 |
| docs/SETUP.md | 배포/환경 | 분기별 |
| docs/ARCHITECTURE.md | 시스템 설계 | 구조 변경 시 |
| docs/DEVELOPMENT.md | 코드 가이드 | 월간 |
| PROJECT_OVERVIEW.md | 프로젝트 개요 | 분기별 |

### 문서 업데이트 규칙

1. 코드 변경 시 관련 문서도 함께 업데이트
2. 새 기능 추가 시 DEVELOPMENT.md에 예시 추가
3. 배포 방식 변경 시 SETUP.md 업데이트
4. 아키텍처 변경 시 ARCHITECTURE.md 수정

## 🎓 코드 스타일 준수

### TypeScript

```typescript
// ✅ 규칙 준수
interface Message {
  id: string;
  role: 'user' | 'assistant';
  content: string;
  timestamp: number;
}

export const getUser = (id: string): User | null => {
  // ...
};

// ❌ 규칙 위반
const getUser = (id: any) => { }; // any 사용
type Message = { ... }; // 타입 대신 인터페이스 사용
```

### React

```typescript
// ✅ 규칙 준수
interface ChatRoomProps {
  title: string;
}

export const ChatRoom: React.FC<ChatRoomProps> = ({ title }) => {
  const [messages, setMessages] = useState<Message[]>([]);

  return <div>{title}</div>;
};

// ❌ 규칙 위반
function ChatRoom(props: any) { } // any 사용, 함수형 아님
class ChatRoom extends React.Component {} // 클래스 컴포넌트
```

### CSS

```css
/* ✅ 규칙 준수 */
.chat-room {
  background-color: #B8D4E8;
  padding: 16px;
}

@media (max-width: 768px) {
  .chat-room {
    padding: 12px;
  }
}

/* ❌ 규칙 위반 */
.chatRoom { } /* 케밥케이스 사용 */
.chat-room {
  background: #B8D4E8; /* 하드코딩 */
}
```

## 🚀 배포 체크리스트

프로덕션 배포 전 확인 사항:

### 코드 품질
- [ ] TypeScript 컴파일 성공
- [ ] ESLint 통과
- [ ] 모든 import 경로 유효
- [ ] 콘솔 경고/에러 없음

### 보안
- [ ] API 키 환경변수 관리
- [ ] key.json .gitignore에 포함
- [ ] CORS 정책 확인
- [ ] 입력값 검증

### 성능
- [ ] 번들 크기 확인
- [ ] 느린 API 응답 처리
- [ ] 메모리 누수 확인

### 문서
- [ ] 배포 가이드 최신화
- [ ] API 문서 최신화
- [ ] 트러블슈팅 가이드 준비

## 🔄 Git 워크플로우

### 브랜치 규칙

```
main (항상 배포 가능한 상태)
  ↓
develop (다음 릴리스 개발)
  ↓
feature/xxx (기능 개발)
fix/xxx (버그 수정)
```

### 커밋 메시지 형식

```
<type>(<scope>): <subject>

<description>

<footer>
```

**Types:**
- feat: 새 기능
- fix: 버그 수정
- docs: 문서 수정
- style: 코드 스타일 (공백, 세미콜론 등)
- refactor: 리팩토링
- perf: 성능 개선
- test: 테스트 추가

**Example:**
```
feat(ChatRoom): add message reactions feature

- Added emoji reaction picker
- Updated MessageBubble to show reactions
- Modified API to support reaction counts

Closes #456
```

## 📊 모니터링 & 유지보수

### 정기 점검 사항

| 주기 | 작업 |
|------|------|
| 주간 | 버그 리포트 확인, 보안 업데이트 |
| 월간 | 의존성 업데이트, 성능 분석 |
| 분기별 | 문서 리뷰, 아키텍처 평가 |
| 연간 | 기술 스택 평가, 마이그레이션 계획 |

### 의존성 업데이트

```bash
# 최신 버전 확인
npm outdated

# 보안 취약점 확인
npm audit

# 업데이트 수행
npm update
npm audit fix
```

## 📞 문의 및 피드백

프로젝트 개선사항:
1. GitHub Issues에 작성
2. Pull Request로 제안
3. 토론은 Discussions 사용

---

**Last Updated**: 2024년 11월 4일
**CLAUDE.md Compliance**: ✅ 100%
